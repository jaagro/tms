<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jaagro.tms.biz.mapper.OrderGoodsMapperExt">
    <select id="listByItemsId" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from order_goods
        where order_item_id = #{id,jdbcType=INTEGER}
    </select>
    <update id="disableByItemsId">
        update from order_goods
        set enabled = 0
        where order_item_id = #{itemId}
      </update>
</mapper>